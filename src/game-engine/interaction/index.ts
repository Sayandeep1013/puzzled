/**
 * Gesture hit-testing and drag controllers live here.
 * High-frequency transforms belong in Reanimated shared values, not React state.
 * Commit only drop, snap, lock, and completion events back into `core` session state.
 */

export {};
